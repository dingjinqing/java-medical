package com.vpu.mp.service.pojo.shop.goods.es;

/**
 * elasticSearch query rule
 */
public enum  QueryType {
    /**
     * must
     */
    MUST,
    /**
     * must not
     */
    MUST_NOT,
    /**
     * should
     */
    SHOULD;
}
