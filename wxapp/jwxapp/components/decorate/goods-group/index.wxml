<view class="goods-group-container" id="{{m.cur_idx}}" style="min-height:{{m.fix_height}}px">
	<!-- 顶部分组 -->
	<block wx:if="{{m.position_style == '0' }}">
		<view class='content_scroll_view'>
			<view style="{{m.fixed ? 'top:'+m.top+'px;position:fixed;z-index:9;width:750rpx;':''}}">
				<scroll-view class='goods_group_scroll' scroll-x='true'>
					<view class='goods_group_nav'>
						<block
						 wx:for="{{m.group_names}}"
						 wx:key="key"
						 wx:for-item="item"
						 wx:for-index='index'
						>
							<view class='group_nav_single {{m.navlen > 4 ? "group_double_flex" : "group_single_flex"}}'>
								<view
								 class='group_nav_single_content'
								 bindtap='bindMenuClick'
								 data-index="{{index}}"
								 data-idx="{{m.idx}}"
								>
									<view class='group_nav_title {{index == m.group_nav_index ? "color_red":" "}}' style='color:{{index == m.group_nav_index ?comColor:""}}'>{{item}}</view>
								</view>
								<view class='nav_bottom_select' style='border-color:{{comColor}}' wx:if="{{index == m.group_nav_index }}" />
							</view>
						</block>
					</view>
				</scroll-view>
			</view>
		</view>
		<view class='goods_group_module clearfix' style="background:{{m.goods_module_bg == 0 ? '': m.goods_bg_color}}">
			<!-- 大图 -->
			<view class='group_big_module' wx:if="{{m.shop_style == 1}}">
				<block
				 data-idx="{{m.idx}}"
				 wx:for="{{m.first_group_goods}}"
				 wx:key='key'
				 wx:for-item="item"
				 wx:for-index='index'
				>
					<!-- 普通商品 -->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="{{item.goods_type == 10 ? 'bindTopre' : 'bindJumpLink'}}"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.cur_idx}}'
					 data-presale_id='{{item.presale_id}}'
					 data-goods_type='{{item.goods_type}}'
					 data-url="pages/item/item?good_id={{item.goods_id}}"
					 wx:if='{{item.goods_type !=3 && item.goods_type !=1 && item.goods_type !=5}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<block wx:if='{{item.goods_type == 10 || item.goods_tags != ""}}'>
									<view class='van-ellipsis'>{{item.goods_name}}</view>
									<view class='title_label'>
										<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
											<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
										</block>
									</view>
								</block>
								<block wx:else>
									<view class='van-two-ellipsis'>{{item.goods_name}}</view>
								</block>
							</view>
							<view class='module_goods_price'>
								<block wx:if="{{m.show_price != 1}}">
									<view class='sale_price' wx:if='{{item.goods_type != 10}}' style='color:{{comColor}};'>￥{{item.reduce_price}}</view>
									<view class='sale_price' wx:if='{{item.goods_type == 10}}' style='color:{{comColor}};'>￥{{item.shop_price}}</view>
								</block>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1}}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2}}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
								<block wx:if="{{m.cart_btn == 0 && item.goods_type != 10 }}">
									<text class='cart_btn iconfont icontianjia' style='font-size:46rpx;color:{{comColor}}' wx:if="{{m.cart_btn_choose == '0'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'/>
									<text class='cart_btn iconfont icongouwuche1' style='font-size:52rpx;color:{{comColor}}' wx:if="{{m.cart_btn_choose == '1'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'/>
									<text class='cart_btn right_buy_btn' style='background:{{comColor}}' wx:if="{{m.cart_btn_choose == '2'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'>马上抢</text>
									<text class='cart_btn cart_buy_btn' style='color:{{comColor}};border:1px solid {{comColor}}' wx:if="{{m.cart_btn_choose == '3'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'>购买</text>
								</block>
							</view>
						</view>
					</view>
					<!-- 拼团 -->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindJumpLink"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 data-url="pages/groupbuyitem/groupbuyitem?pin_group_id={{m.pin_group_goods[item.goods_id].pin_activity_id}}"
					 wx:if='{{m.pin_group_goods[item.goods_id] && item.goods_type ==1}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
							<view class='save_money' wx:if='{{m.pin_group_goods[item.goods_id]}}' style='background:-webkit-linear-gradient(left,{{linColor}},{{comColor1}})'>开团立省{{m.pin_group_goods[item.goods_id].less_money}}元</view>
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<view class='van-ellipsis'>{{item.goods_name}}</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}};'>￥{{m.pin_group_goods[item.goods_id].pin_group_price}}</view>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1}}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2}}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
							</view>

						</view>
					</view>
					<!-- 砍价-->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindToKanJia"
					 data-zhujian="{{index}}"
					 data-is_prd='{{item.is_prd}}'
					 data-link="pages/bargainitem/bargainitem?bargain_id={{item.bargin_id}}"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 wx:if='{{item.goods_type ==3}}'
					 data-group='{{1}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
							<view class='save_money kanjia_money' style='background:-webkit-linear-gradient(left,{{linColor}},{{comColor1}})'>{{item.bargain_price}}元砍走</view>
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<view class='van-ellipsis'>{{item.goods_name}}</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}};'>￥{{item.bargain_price}}</view>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1}}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2}}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
							</view>

						</view>
					</view>
					<!-- 秒杀-->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindToSecKill"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-sk_id='{{item.sk_id}}'
					 data-idx='{{m.idx}}'
					 data-link="pages/seckillitem/seckillitem?sk_id={{item.sk_id}}"
					 wx:if='{{ item.goods_type ==5 }}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<view class='van-ellipsis'>{{item.goods_name}}</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}};'>￥{{item.seckill_price}}</view>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1}}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2}}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
							</view>

						</view>
					</view>
				</block>
			</view>
			<!-- 双列图 -->
			<view class='group_double_module' wx:if="{{m.shop_style == 2}}">
				<block
				 data-idx="{{m.idx}}"
				 wx:for="{{m.first_group_goods}}"
				 wx:key='key'
				 wx:for-item="item"
				 wx:for-index='index'
				>
					<!-- 普通商品 -->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="{{item.goods_type == 10 ? 'bindTopre' : 'bindJumpLink'}}"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 data-presale_id='{{item.presale_id}}'
					 data-goods_type='{{item.goods_type}}'
					 data-url="pages/item/item?good_id={{item.goods_id}}"
					 wx:if='{{item.goods_type !=3 && item.goods_type !=1 && item.goods_type !=5}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<block wx:if='{{item.goods_type == 10 || item.goods_tags != ""}}'>
									<view class='van-ellipsis'>{{item.goods_name}}</view>
									<view class='title_label'>
										<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
											<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
										</block>
									</view>
								</block>
								<block wx:else>
									<view class='van-two-ellipsis'>{{item.goods_name}}</view>
								</block>

							</view>
							<view class='module_goods_price'>
								<block wx:if="{{m.show_price != 1}}">
									<view class='sale_price' wx:if='{{item.goods_type != 10}}' style='color:{{comColor}};'>￥{{item.reduce_price}}</view>
									<view class='sale_price' wx:if='{{item.goods_type == 10}}' style='color:{{comColor}};'>￥{{item.shop_price}}</view>
								</block>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1 && item.price_len < 16 }}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2 && item.real_len < 9 }}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3 && item.real_len < 9}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
								<block wx:if="{{m.cart_btn == 0 && item.goods_type != 10 }}">
									<text class='cart_btn iconfont icontianjia' style='font-size:42rpx;color:{{comColor}}' wx:if="{{m.cart_btn_choose == '0'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'/>
									<text class='cart_btn iconfont icongouwuche1' style='font-size:42rpx;color:{{comColor}}' wx:if="{{m.cart_btn_choose == '1'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'/>
									<text class='cart_btn right_buy_btn' style='background:{{comColor}}' wx:if="{{m.cart_btn_choose == '2'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'>马上抢</text>
									<text class='cart_btn cart_buy_btn' style='color:{{comColor}};border:1px solid {{comColor}}' wx:if="{{m.cart_btn_choose == '3'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'>购买</text>
								</block>
							</view>
						</view>
					</view>
					<!-- 拼团 -->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindJumpLink"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 data-url="pages/groupbuyitem/groupbuyitem?pin_group_id={{m.pin_group_goods[item.goods_id].pin_activity_id}}"
					 wx:if='{{m.pin_group_goods[item.goods_id] && item.goods_type ==1}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
							<view class='save_money' wx:if='{{m.pin_group_goods[item.goods_id]}}' style='background:-webkit-linear-gradient(left,{{linColor}},{{comColor1}})'>开团立省{{m.pin_group_goods[item.goods_id].less_money}}元</view>
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<view class='van-ellipsis'>{{item.goods_name}}</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}}'>￥{{m.pin_group_goods[item.goods_id].pin_group_price}}</view>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1 && item.price_len < 16 }}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2 && item.real_len < 9 }}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3 && item.real_len < 9}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
							</view>

						</view>
					</view>
					<!-- 砍价-->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindToKanJia"
					 data-zhujian="{{index}}"
					 data-is_prd='{{item.is_prd}}'
					 data-link="pages/bargainitem/bargainitem?bargain_id={{item.bargin_id}}"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 wx:if='{{item.goods_type ==3}}'
					 data-group='{{1}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
							<view class='save_money kanjia_money' style='background:-webkit-linear-gradient(left,{{linColor}},{{comColor1}})'>{{item.bargain_price}}元砍走</view>
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<view class='van-ellipsis'>{{item.goods_name}}</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}}'>￥{{item.bargain_price}}</view>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1 && item.price_len < 16 }}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2 && item.real_len < 9 }}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3 && item.real_len < 9}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
							</view>
						</view>
					</view>
					<!-- 秒杀-->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindToSecKill"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-sk_id='{{item.sk_id}}'
					 data-idx='{{m.idx}}'
					 data-link="pages/seckillitem/seckillitem?sk_id={{item.sk_id}}"
					 wx:if='{{ item.goods_type ==5 }}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<view class='van-ellipsis'>{{item.goods_name}}</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}}'>￥{{item.seckill_price}}</view>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1 && item.price_len < 16 }}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2 && item.real_len < 9 }}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3 && item.real_len < 9}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
							</view>

						</view>
					</view>
				</block>
			</view>
			<!-- 三列图 -->
			<view class='group_trible_module' wx:if="{{m.shop_style == 3}}">
				<block
				 data-idx="{{m.idx}}"
				 wx:for="{{m.first_group_goods}}"
				 wx:key='key'
				 wx:for-item="item"
				 wx:for-index='index'
				>
					<!-- 普通商品 -->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="{{item.goods_type == 10 ? 'bindTopre' : 'bindJumpLink'}}"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 data-presale_id='{{item.presale_id}}'
					 data-goods_type='{{item.goods_type}}'
					 data-url="pages/item/item?good_id={{item.goods_id}}"
					 wx:if='{{item.goods_type !=3 && item.goods_type !=1 && item.goods_type !=5}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<!-- <view class='reduce_price' wx:if="{{item.goods_price_action == 2 && item.goods_type != 10}}" style='background:{{linColor}};color:#fff'>限时降价，立即查看</view>
							<view class='reduce_price' wx:elif="{{item.goods_price_action == 3}}" style='background:{{linColor}};color:#fff'>新人专享，首单特惠</view>
							<view class='exclusive' wx:elif="{{item.is_card_exclusive == 1 && item.goods_type != 1 && item.goods_type != 3 && item.goods_type != 5 && item.goods_type != 10 && item.goods_price_action != 2 && item.goods_price_action != 3}}">会员专享商品，立即查看</view>
							<view class='grade' wx:elif="{{item.goods_price_action == 1 && item.goods_type != 10}}">专享会员价，立即查看</view> -->
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<block wx:if='{{item.goods_type == 10 || item.goods_tags != ""}}'>
									<view class='van-ellipsis'>{{item.goods_name}}</view>
									<view class='title_label'>
										<block wx:if='{{item.goods_tags[0] != ""}}'>
											<view class='pin_flag' wx:if='{{item.goods_tags[0] != "会员专享" && item.goods_tags[0] != "会员价" }}' style='color:{{comColor}};border-color:{{comColor}}'>{{item.goods_tags[0]}}</view>
											<view class='member_icon' wx:else>{{item.goods_tags[0]}}</view>
										</block>

									</view>
								</block>
								<block wx:else>
									<view class='van-two-ellipsis'>{{item.goods_name}}</view>
								</block>

							</view>
							<view class='module_goods_price'>
								<block wx:if="{{m.show_price != 1}}">
									<view class='sale_price' wx:if='{{item.goods_type != 10}}' style='color:{{comColor}};'>￥{{item.reduce_price}}</view>
									<view class='sale_price' wx:if='{{item.goods_type == 10}}' style='color:{{comColor}};'>￥{{item.shop_price}}</view>
								</block>
								<block wx:if="{{m.cart_btn == 0 && item.goods_type != 10}}">
									<text class='cart_btn iconfont icontianjia' style='font-size:32rpx;color:{{comColor}};margin-top:4rpx;' wx:if="{{m.cart_btn_choose == '0'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'/>
									<text class='cart_btn iconfont icongouwuche1' style='font-size:32rpx;color:{{comColor}};margin-top:4rpx;' wx:if="{{m.cart_btn_choose == '1'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'/>

								</block>
							</view>
						</view>
					</view>
					<!-- 拼团 -->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindJumpLink"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 data-url="pages/groupbuyitem/groupbuyitem?pin_group_id={{m.pin_group_goods[item.goods_id].pin_activity_id}}"
					 wx:if='{{m.pin_group_goods[item.goods_id] && item.goods_type ==1}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
							<view class='save_money' wx:if='{{m.pin_group_goods[item.goods_id]}}' style='background:-webkit-linear-gradient(left,{{linColor}},{{comColor1}})'>开团立省{{m.pin_group_goods[item.goods_id].less_money}}元</view>
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<view class='van-ellipsis'>{{item.goods_name}}</view>
								<view class='title_label'>
									<view class='pin_flag' style='color:{{comColor}};border-color:{{comColor}}'>拼团</view>
								</view>
							</view>
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}}'>￥{{m.pin_group_goods[item.goods_id].pin_group_price}}</view>
							</view>
						</view>
					</view>
					<!-- 砍价-->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindToKanJia"
					 data-zhujian="{{index}}"
					 data-is_prd='{{item.is_prd}}'
					 data-link="pages/bargainitem/bargainitem?bargain_id={{item.bargin_id}}"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 wx:if='{{item.goods_type ==3}}'
					 data-group='{{1}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
							<view class='save_money kanjia_money' style='background:-webkit-linear-gradient(left,{{linColor}},{{comColor1}})'>{{item.bargain_price}}元砍走</view>
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<view class='van-ellipsis'>{{item.goods_name}}</view>
								<view class='title_label'>
									<view class='pin_flag' style='color:{{comColor}};border-color:{{comColor}}'>砍价</view>
								</view>

							</view>
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}}'>￥{{item.bargain_price}}</view>
							</view>
						</view>
					</view>
					<!-- 秒杀-->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindToSecKill"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-sk_id='{{item.sk_id}}'
					 data-idx='{{m.idx}}'
					 data-link="pages/seckillitem/seckillitem?sk_id={{item.sk_id}}"
					 wx:if='{{ item.goods_type ==5 }}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<view class='van-ellipsis'>{{item.goods_name}}</view>
								<view class='title_label'>
									<view class='pin_flag' style='color:{{comColor}};border-color:{{comColor}}'>秒杀</view>
								</view>

							</view>
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}};'>￥{{item.seckill_price}}</view>
							</view>
						</view>
					</view>
				</block>
			</view>
			<!-- 横向滑动 -->
			<scroll-view class='group_slide_module' scroll-x='true' wx:if="{{m.shop_style == 5 && m.first_group_goods != ''}}">
				<block
				 data-idx="{{m.idx}}"
				 wx:for="{{m.first_group_goods}}"
				 wx:key='key'
				 wx:for-item="item"
				 wx:for-index='index'
				>
					<!-- 普通商品 -->
					<view
					 class='group_container {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="{{item.goods_type == 10 ? 'bindTopre' : 'bindJumpLink'}}"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 data-presale_id='{{item.presale_id}}'
					 data-goods_type='{{item.goods_type}}'
					 data-url="pages/item/item?good_id={{item.goods_id}}"
					 wx:if='{{item.goods_type !=3 && item.goods_type !=1 && item.goods_type !=5}}'
					>
						<view
						 class="see_more"
						 catchtap='bindMenuClick'
						 data-click='{{1}}'
						 data-index="{{index}}"
						 data-idx="{{m.idx}}"
						 wx:if="{{m.more_flag == 1 && index == 6 }}"
						>
							<view>查看更多</view>
							<view class="see_line" />
							<view style="line-height:20rpx;">see more</view>
						</view>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<block wx:if='{{item.goods_type == 10 || item.goods_tags != ""}}'>
									<view class='van-ellipsis'>{{item.goods_name}}</view>
									<view class='title_label'>
										<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
											<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
										</block>
									</view>
								</block>
								<block wx:else>
									<view class='van-two-ellipsis' style="white-space:normal;">{{item.goods_name}}</view>
								</block>

							</view>
							<view class='module_goods_price'>
								<block wx:if="{{m.show_price != 1}}">
									<view class='sale_price' wx:if='{{item.goods_type != 10}}' style='color:{{comColor}};'>￥{{item.reduce_price}}</view>
									<view class='sale_price' wx:if='{{item.goods_type == 10}}' style='color:{{comColor}};'>￥{{item.shop_price}}</view>
								</block>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1 && item.price_len < 14 }}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2 && item.real_len < 8 }}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3 && item.real_len < 8}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
								<block wx:if="{{m.cart_btn == 0 && item.goods_type != 10 }}">
									<text class='cart_btn iconfont icontianjia' style='font-size:32rpx;color:{{comColor}};margin-top:3rpx;' wx:if="{{m.cart_btn_choose == '0'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'/>
									<text class='cart_btn iconfont icongouwuche1' style='font-size:32rpx;color:{{comColor}};margin-top:3rpx;' wx:if="{{m.cart_btn_choose == '1'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'/>

								</block>
							</view>
						</view>
					</view>
					<!-- 拼团 -->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindJumpLink"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 data-url="pages/groupbuyitem/groupbuyitem?pin_group_id={{m.pin_group_goods[item.goods_id].pin_activity_id}}"
					 wx:if='{{m.pin_group_goods[item.goods_id] && item.goods_type ==1}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
							<view class='save_money' wx:if='{{m.pin_group_goods[item.goods_id]}}' style='background:-webkit-linear-gradient(left,{{linColor}},{{comColor1}})'>开团立省{{m.pin_group_goods[item.goods_id].less_money}}元</view>
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<view class='van-ellipsis'>{{item.goods_name}}</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}};'>￥{{m.pin_group_goods[item.goods_id].pin_group_price}}</view>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1 && item.price_len < 14 }}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2 && item.real_len < 8 }}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3 && item.real_len < 8}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
							</view>

						</view>
					</view>
					<!-- 砍价-->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindToKanJia"
					 data-zhujian="{{index}}"
					 data-is_prd='{{item.is_prd}}'
					 data-link="pages/bargainitem/bargainitem?bargain_id={{item.bargin_id}}"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 wx:if='{{item.goods_type ==3}}'
					 data-group='{{1}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
							<view class='save_money kanjia_money' style='background:-webkit-linear-gradient(left,{{linColor}},{{comColor1}})'>{{item.bargain_price}}元砍走</view>
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<view class='van-ellipsis'>{{item.goods_name}}</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>

								<view class='sale_price' style='color:{{comColor}};'>￥{{item.bargain_price}}</view>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1 && item.price_len < 14 }}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2 && item.real_len < 8 }}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3 && item.real_len < 8}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
							</view>

						</view>
					</view>
					<!-- 秒杀-->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindToSecKill"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-sk_id='{{item.sk_id}}'
					 data-idx='{{m.idx}}'
					 data-link="pages/seckillitem/seckillitem?sk_id={{item.sk_id}}"
					 wx:if='{{ item.goods_type ==5 }}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
						</view>
						<view class='module_text'>
							<view class='module_goods_name' style='display:{{m.show_name == 1 ? "none":"block"}}'>
								<view class='van-ellipsis'>{{item.goods_name}}</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>

								<view class='sale_price' style='color:{{comColor}};'>￥{{item.seckill_price}}</view>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1 && item.price_len < 14 }}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2 && item.real_len < 8 }}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3 && item.real_len < 8}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
							</view>

						</view>
					</view>
				</block>
			</scroll-view>
			<!-- 商品列表 -->
			<view class='group_list_module' wx:if="{{m.shop_style == 4}}">
				<block
				 data-idx="{{m.idx}}"
				 wx:for="{{m.first_group_goods}}"
				 wx:key='key'
				 wx:for-item="item"
				 wx:for-index='index'
				>
					<!-- 普通商品 -->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="{{item.goods_type == 10 ? 'bindTopre' : 'bindJumpLink'}}"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 data-presale_id='{{item.presale_id}}'
					 data-goods_type='{{item.goods_type}}'
					 data-url="pages/item/item?good_id={{item.goods_id}}"
					 wx:if='{{item.goods_type !=3 && item.goods_type !=1 && item.goods_type !=5}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
						</view>
						<view class='module_text'>
							<view class='module_goods_name' wx:if='{{m.show_name != 1}}'>
								<view class='top_title'>
									<view class='van-two-ellipsis'>{{item.goods_name}}</view>
								</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>
							</view>
							<view class='module_goods_name' wx:if='{{m.show_name == 1}}' />
							<view class='module_goods_price'>
								<block wx:if="{{m.show_price != 1}}">
									<view class='sale_price' wx:if='{{item.goods_type != 10}}' style='color:{{comColor}};'>￥{{item.reduce_price}}</view>
									<view class='sale_price' wx:if='{{item.goods_type == 10}}' style='color:{{comColor}};'>￥{{item.shop_price}}</view>
								</block>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1 && item.price_len < 14 }}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2 && item.real_len < 8 }}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3 && item.real_len < 8}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
								<block wx:if="{{m.cart_btn == 0 && item.goods_type != 10 }}">
									<text class='cart_btn iconfont icontianjia' style='font-size:46rpx;color:{{comColor}}' wx:if="{{m.cart_btn_choose == '0'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'/>
									<text class='cart_btn iconfont icongouwuche1' style='font-size:46rpx;color:{{comColor}}' wx:if="{{m.cart_btn_choose == '1'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'/>
									<text class='cart_btn right_buy_btn' style='background:{{comColor}}' wx:if="{{m.cart_btn_choose == '2'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'>马上抢</text>
									<text class='cart_btn cart_buy_btn' style='color:{{comColor}};border:1px solid {{comColor}}' wx:if="{{m.cart_btn_choose == '3'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'>购买</text>
								</block>
							</view>
						</view>
					</view>
					<!-- 拼团 -->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindJumpLink"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 data-url="pages/groupbuyitem/groupbuyitem?pin_group_id={{m.pin_group_goods[item.goods_id].pin_activity_id}}"
					 wx:if='{{m.pin_group_goods[item.goods_id] && item.goods_type ==1}}'
					 data-group='{{1}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
							<view class='save_money' wx:if='{{m.pin_group_goods[item.goods_id]}}' style='background:-webkit-linear-gradient(left,{{linColor}},{{comColor1}})'>开团立省{{m.pin_group_goods[item.goods_id].less_money}}元</view>
						</view>
						<view class='module_text'>
							<view class='module_goods_name' wx:if='{{m.show_name != 1}}'>
								<view class='top_title'>
									<view class='van-two-ellipsis'>{{item.goods_name}}</view>
								</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:key='key1' wx:for-item='it'>
										<view class='pin_flag' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_name' wx:if='{{m.show_name == 1}}' />
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}}'>￥{{m.pin_group_goods[item.goods_id].pin_group_price}}</view>
							</view>
							<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
								<block wx:if="{{m.show_market == 1 && item.price_len < 14 }}">
									<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
								</block>
								<block wx:if="{{m.show_market == 2 && item.real_len < 8 }}">
									<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
								</block>
								<block wx:if="{{m.show_market == 3 && item.real_len < 8}}">
									<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
								</block>
							</block>
						</view>
					</view>
					<!-- 砍价-->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindToKanJia"
					 data-zhujian="{{index}}"
					 data-is_prd='{{item.is_prd}}'
					 data-link="pages/bargainitem/bargainitem?bargain_id={{item.bargin_id}}"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 wx:if='{{item.goods_type ==3}}'
					 data-group='{{1}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
							<view class='save_money kanjia_money' style='background:-webkit-linear-gradient(left,{{linColor}},{{comColor1}})'>{{item.bargain_price}}元砍走</view>
						</view>
						<view class='module_text'>
							<view class='module_goods_name' wx:if='{{m.show_name != 1}}'>
								<view class='top_title'>
									<view class='van-two-ellipsis'>{{item.goods_name}}</view>
								</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_name' wx:if='{{m.show_name == 1}}' />
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}}'>￥{{item.bargain_price}}</view>
							</view>
							<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
								<block wx:if="{{m.show_market == 1 && item.price_len < 14 }}">
									<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
								</block>
								<block wx:if="{{m.show_market == 2 && item.real_len < 8 }}">
									<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
								</block>
								<block wx:if="{{m.show_market == 3 && item.real_len < 8}}">
									<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
								</block>
							</block>
						</view>
					</view>
					<!-- 秒杀-->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindToSecKill"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-sk_id='{{item.sk_id}}'
					 data-idx='{{m.idx}}'
					 data-link="pages/seckillitem/seckillitem?sk_id={{item.sk_id}}"
					 wx:if='{{ item.goods_type ==5 }}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
						</view>
						<view class='module_text'>
							<view class='module_goods_name' wx:if='{{m.show_name != 1}}'>
								<view class='top_title'>
									<view class='van-two-ellipsis'>{{item.goods_name}}</view>
								</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_name' wx:if='{{m.show_name == 1}}' />
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}}'>￥{{item.seckill_price}}</view>
							</view>
							<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
								<block wx:if="{{m.show_market == 1 && item.price_len < 14 }}">
									<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
								</block>
								<block wx:if="{{m.show_market == 2 && item.real_len < 8 }}">
									<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
								</block>
								<block wx:if="{{m.show_market == 3 && item.real_len < 8}}">
									<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
								</block>
							</block>
						</view>
					</view>
				</block>
			</view>
		</view>
		<view
		 class='click_see_more'
		 catchtap='bindMenuClick'
		 data-click='{{1}}'
		 data-index="{{index}}"
		 data-idx="{{m.idx}}"
		 wx:if="{{m.more_flag == 1 && m.shop_style != 5}}"
		>
			<view class='click_see'>
				<text>查看更多</text>
				<image lazy-load='{{true}}' src='{{m.imgUrl}}image/wxapp/see_more_down.png' />
			</view>
		</view>
	</block>
	<!-- 左部分组 -->
	<block wx:if="{{m.position_style == '1' }}">
		<view class='left_container' style='height:{{m.scr_height}}px'>
			<view class='left_nav'>
				<block
				 wx:for='{{m.group_names}}'
				 wx:for-item='item'
				 wx:key='key'
				 wx:for-index="index"
				>
					<view
					 class='left_icon {{m.group_nav_index == index ? "left_select" : ""}}'
					 style='border-left-color:{{m.group_nav_index == index ? comColor:""}}'
					 bindtap='bindMenuClick'
					 data-index="{{index}}"
					 data-idx="{{m.idx}}"
					>
						<view class='left_title'>{{item}}</view>
					</view>
				</block>
			</view>
			<view class='goods_left_module' style="background:{{m.goods_module_bg == 0 ? '': m.goods_bg_color}}">
				<!-- 普通商品 -->
				<block
				 wx:for='{{m.group_names}}'
				 wx:for-item='item'
				 wx:key='key'
				 wx:for-index="index"
				>
					<view class='left_module_title' wx:if="{{index == m.group_nav_index}}">
						{{item}}
					</view>
				</block>

				<block
				 data-idx="{{m.idx}}"
				 wx:for="{{m.first_group_goods}}"
				 wx:key='key'
				 wx:for-item="item"
				 wx:for-index='index'
				>
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="{{item.goods_type == 10 ? 'bindTopre' : 'bindJumpLink'}}"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 data-presale_id='{{item.presale_id}}'
					 data-goods_type='{{item.goods_type}}'
					 data-url="pages/item/item?good_id={{item.goods_id}}"
					 wx:if='{{item.goods_type !=3 && item.goods_type !=1 && item.goods_type !=5}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
						</view>
						<view class='module_text'>
							<view class='module_goods_name' wx:if='{{m.show_name != 1}}'>

								<view class='top_title'>
									<view class='van-two-ellipsis'>{{item.goods_name}}</view>
								</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>
							</view>
							<view class='module_goods_name' wx:if="{{m.show_name == 1}}" />
							<view class='module_goods_price'>
								<block wx:if="{{m.show_price != 1}}">
									<view class='sale_price' wx:if='{{item.goods_type != 10}}' style='color:{{comColor}};'>￥{{item.reduce_price}}</view>
									<view class='sale_price' wx:if='{{item.goods_type == 10}}' style='color:{{comColor}};'>￥{{item.shop_price}}</view>
								</block>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1 && item.price_len < 14 }}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2 && item.real_len < 8 }}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3 && item.real_len < 8}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
								<block wx:if="{{m.cart_btn == 0 && item.goods_type != 10 }}">
									<text class='cart_btn iconfont icontianjia' style='font-size:46rpx;color:{{comColor}}' wx:if="{{m.cart_btn_choose == '0'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'/>
									<text class='cart_btn iconfont icongouwuche1' style='font-size:46rpx;color:{{comColor}}' wx:if="{{m.cart_btn_choose == '1'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'/>
									<text class='cart_btn right_buy_btn' style='background:{{comColor}}' wx:if="{{m.cart_btn_choose == '2'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'>马上抢</text>
									<text class='cart_btn cart_buy_btn' style='color:{{comColor}};border:1px solid {{comColor}}' wx:if="{{m.cart_btn_choose == '3'}}" catchtap='bindAddCart' data-goods_id='{{item.goods_id}}'>购买</text>
								</block>
							</view>
						</view>
					</view>
					<!-- 拼团 -->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindJumpLink"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 data-url="pages/groupbuyitem/groupbuyitem?pin_group_id={{m.pin_group_goods[item.goods_id].pin_activity_id}}"
					 wx:if='{{m.pin_group_goods[item.goods_id] && item.goods_type ==1}}'
					 data-group='{{1}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
							<view class='save_money' wx:if='{{m.pin_group_goods[item.goods_id]}}' style='background:-webkit-linear-gradient(left,{{linColor}},{{comColor1}})'>开团立省{{m.pin_group_goods[item.goods_id].less_money}}元</view>
						</view>
						<view class='module_text'>
							<view class='module_goods_name' wx:if='{{m.show_name != 1}}'>
								<view class='top_title'>
									<view class='van-two-ellipsis'>{{item.goods_name}}</view>
								</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_name' wx:if='{{m.show_name == 1}}' />
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>
								<view class='sale_price' style='color:{{comColor}}'>￥{{m.pin_group_goods[item.goods_id].pin_group_price}}</view>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1 && item.price_len < 14 }}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2 && item.real_len < 8 }}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3 && item.real_len < 8}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
							</view>
						</view>
					</view>
					<!-- 砍价-->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindToKanJia"
					 data-zhujian="{{index}}"
					 data-is_prd='{{item.is_prd}}'
					 data-link="pages/bargainitem/bargainitem?bargain_id={{item.bargin_id}}"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-idx='{{m.idx}}'
					 wx:if='{{item.goods_type ==3}}'
					 data-group='{{1}}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
							<view class='save_money kanjia_money' style='background:-webkit-linear-gradient(left,{{linColor}},{{comColor1}})'>{{item.bargain_price}}元砍走</view>
						</view>
						<view class='module_text'>
							<view class='module_goods_name' wx:if='{{m.show_name != 1}}'>
								<view class='top_title'>
									<view class='van-two-ellipsis'>{{item.goods_name}}</view>
								</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_name' wx:if='{{m.show_name == 1}}' />
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>

								<view class='sale_price' style='color:{{comColor}}'>￥{{item.bargain_price}}</view>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1 && item.price_len < 14 }}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2 && item.real_len < 8 }}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3 && item.real_len < 8}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
							</view>
						</view>
					</view>
					<!-- 秒杀-->
					<view
					 class='group_container  {{m.if_radius == 1 ? "circle_radius": " "}} {{m.module_style == 2 ? "have_shadow":" "}} {{m.module_style == 3 ? "have_border" : ""}}'
					 catchtap="bindToSecKill"
					 data-is_first_touch="{{m.is_first_touch}}"
					 data-sk_id='{{item.sk_id}}'
					 data-idx='{{m.idx}}'
					 data-link="pages/seckillitem/seckillitem?sk_id={{item.sk_id}}"
					 wx:if='{{ item.goods_type ==5 }}'
					>
						<view class='module_img'>
							<include src="../goodscommon/labelcommon.wxml" />
							<image class="sell_out" wx:if='{{item.goods_number == 0}}' src="{{m.imgUrl}}image/wxapp/goods_sell_out.png" />
							<view class='group_delete' wx:if='{{item.is_delete == 1}}'>商品已删除</view>
							<image lazy-load='{{true}}' src='{{item.goods_img}}!big' />
						</view>
						<view class='module_text'>
							<view class='module_goods_name' wx:if='{{m.show_name != 1}}'>
								<view class='top_title'>
									<view class='van-two-ellipsis'>{{item.goods_name}}</view>
								</view>
								<view class='title_label'>
									<block wx:for='{{item.goods_tags}}' wx:for-item='it' wx:key='key1'>
										<view class='{{it != "会员专享" && it != "会员价" ? "pin_flag" : "member_icon"}}' style='color:{{comColor}};border-color:{{comColor}}'>{{it}}</view>
									</block>
								</view>

							</view>
							<view class='module_goods_name' wx:if='{{m.show_name == 1}}' />
							<view class='module_goods_price' style='display:{{m.show_price == 1?"none":"block"}}'>

								<view class='sale_price' style='color:{{comColor}}'>￥{{item.seckill_price}}</view>
								<block wx:if='{{m.other_message == 0  && m.cart_btn != 0}}'>
									<block wx:if="{{m.show_market == 1 && item.price_len < 14 }}">
										<view class='market_price' wx:if="{{item.line_price != undefined && item.line_price != '0.00'}}">￥{{item.line_price}}</view>
									</block>
									<block wx:if="{{m.show_market == 2 && item.real_len < 8 }}">
										<view class='market_price line-through-none'>{{item.goods_sale_num > 999 ? '999+' : item.goods_sale_num}}人付款</view>
									</block>
									<block wx:if="{{m.show_market == 3 && item.real_len < 8}}">
										<view class='market_price line-through-none'>{{item.comment_num > 999 ? '999+' : item.comment_num}}人评价</view>
									</block>
								</block>
							</view>
						</view>
					</view>
				</block>
				<view
				 class='click_see_more'
				 catchtap='bindMenuClick'
				 data-click='{{1}}'
				 data-index="{{index}}"
				 data-idx="{{m.idx}}"
				 wx:if="{{m.more_flag == 1}}"
				>
					<view class='click_see'>
						<text>查看更多</text>
						<image lazy-load='{{true}}' src='{{m.imgUrl}}image/wxapp/see_more_down.png' />
					</view>
				</view>
			</view>
		</view>
	</block>
</view>

